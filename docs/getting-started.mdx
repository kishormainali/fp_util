# Getting Started

### Installation

```shell
  flutter pub add fp_util
```

or add the following to your `pubspec.yaml`:

```yaml
dependencies:
  fp_util: latest
```

and run `flutter pub get`

### Import

```dart
import 'package:fp_util/fp_util.dart';
```