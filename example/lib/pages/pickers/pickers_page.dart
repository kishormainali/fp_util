import 'package:flutter/material.dart';

class PickersPage extends StatelessWidget {
  const PickersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PickersPage'),
      ),
    );
  }
}
